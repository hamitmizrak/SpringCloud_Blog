# Dikkat: account-service projesiyle account-service.yml aynı isimde olması gerekiyor

server:
  port: 8501

spcloud:
  cassandra:
    contact:
      point: 127.0.0.1
    port : 9042
    keyspace:
      name: pachysandra

    username : cassandra
    password : cassandra